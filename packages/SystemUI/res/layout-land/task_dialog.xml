<?xml version="1.0" encoding="utf-8"?>
<FrameLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:layout_alignParentBottom="true"
    android:layout_centerHorizontal="true"
    android:id="@+id/frameLayout"
    android:background="@drawable/taskviewbg"
    >

    <LinearLayout
        android:orientation="horizontal"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_gravity="center_horizontal|top"
        android:layout_marginLeft="@dimen/systemui_size_sixteen"
        android:layout_marginRight="@dimen/systemui_size_sixteen"
        android:layout_marginTop="@dimen/systemui_size_sixteen"
        android:layout_marginBottom="@dimen/systemui_size_sixteen"
        >
        <GridView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:id="@+id/gridView"
            android:layout_alignParentTop="true"
            android:layout_alignParentLeft="true"
            android:layout_alignParentStart="true"
            android:verticalSpacing="@dimen/systemui_size_sixteen"
            android:horizontalSpacing="@dimen/systemui_size_sixteen"
            android:numColumns="auto_fit"
            android:stretchMode="columnWidth"
            android:gravity="center"
            />

    </LinearLayout>
</FrameLayout>
